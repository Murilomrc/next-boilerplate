import { useContext } from 'react'
/* import { AuthContext, AuthProviderType } from '../contexts/AuthContext'

export default function useAuth() {
    return useContext(AuthContext) as AuthProviderType
}
 */
